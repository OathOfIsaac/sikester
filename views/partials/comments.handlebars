  {{#if comments.length}}

<div class="single-posts">
  <div class="title">
    <p class="font-weight-bold">By: {{user.username}} {{format_date created_at}}</p>
  </div>
  <div class="meta p-3">
    <p>{{> comments post.comments}}</p>
  </div>
</div>

{{/if}} 